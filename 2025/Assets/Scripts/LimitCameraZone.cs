using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LimitCameraZone : MonoBehaviour
{
    /*#region references
    private Camera _myCamera;
    //private CameraMovementController _myCameraMovementController;
    #endregion

    #region methods
    private void OnTriggerExit2D(Collider2D collision)
    {
        Player_Life_Component hitPlayer = collision.gameObject.GetComponent<Player_Life_Component>();
        if (hitPlayer)
        {
            Debug.Log("He salido");
            _myCamera.StopCamera();
            //_myCameraMovementController.StopCamera();
            
        }
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        Player_Life_Component hitPlayer = collision.gameObject.GetComponent<Player_Life_Component>();
        if (hitPlayer)
        {
            Debug.Log("He entrado");
            _myCamera.ReanudeCamera();
            //_myCameraMovementController.ReanudeCamera();
            
        }
    }
    #endregion
    // Start is called before the first frame update
    void Start()
    {
        _myCamera = Camera.main;
        //_myCameraMovementController = _myCameraMovementController.GetComponent<CameraMovementController>();
    }

    // Update is called once per frame
    void Update()
    {

    }*/
}
